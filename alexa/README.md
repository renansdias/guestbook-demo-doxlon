# Intents

```
{
  "intents": [
    {
      "intent": "DeployGuestbookProdIntent"
    },
    {
      "intent": "DeployGuestbookStagIntent"
    },
    {
      "intent": "RollbackGuestbookProdIntent"
    },
    {
      "intent": "RollbackGuestbookStagIntent"
    },
    {
      "intent": "AMAZON.YesIntent"
    },
    {
      "intent": "AMAZON.NoIntent"
    }
  ]
}
```

# Uterrances

```
DeployGuestbookProdIntent deploy the guestbook application to production
DeployGuestbookProdIntent deploy the guestbook application to duck
DeployGuestbookProdIntent deploy the guestbook app to production
DeployGuestbookProdIntent deploy the guestbook app to duck
DeployGuestbookProdIntent deploy the guestbook application to prod
DeployGuestbookProdIntent deploy the guestbook application to prod
DeployGuestbookProdIntent deploy the guestbook app to prod
DeployGuestbookProdIntent deploy the guestbook app to prod

DeployGuestbookStagIntent deploy the guestbook application to staging
DeployGuestbookStagIntent deploy the guestbook application to june
DeployGuestbookStagIntent deploy the guestbook application to gin
DeployGuestbookStagIntent deploy the guestbook application to stay
DeployGuestbookStagIntent deploy the guestbook application to stay gin
DeployGuestbookStagIntent deploy the guestbook app to staging
DeployGuestbookStagIntent deploy the guestbook app to june
DeployGuestbookStagIntent deploy the guestbook app to gin
DeployGuestbookStagIntent deploy the guestbook app to stay
DeployGuestbookStagIntent deploy the guestbook app to stay gin

RollbackGuestbookProdIntent rollback the guestbook application on production
RollbackGuestbookProdIntent rollback the guestbook application on duck
RollbackGuestbookProdIntent rollback the guestbook application on prod
RollbackGuestbookProdIntent rollback the guestbook application on uction
RollbackGuestbookProdIntent rollback the guestbook app on production
RollbackGuestbookProdIntent rollback the guestbook app on duck
RollbackGuestbookProdIntent rollback the guestbook app on prod
RollbackGuestbookProdIntent rollback the guestbook app on uction

RollbackGuestbookStagIntent rollback the guestbook application on staging
RollbackGuestbookStagIntent rollback the guestbook application on june
RollbackGuestbookStagIntent rollback the guestbook application on gin
RollbackGuestbookStagIntent rollback the guestbook application on stay
RollbackGuestbookStagIntent rollback the guestbook application on stay gin
RollbackGuestbookStagIntent rollback the guestbook app on staging
RollbackGuestbookStagIntent rollback the guestbook app on june
RollbackGuestbookStagIntent rollback the guestbook app on gin
RollbackGuestbookStagIntent rollback the guestbook app on stay
RollbackGuestbookStagIntent rollback the guestbook app on stay gin
```